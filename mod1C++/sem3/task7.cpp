#include <iostream>
#include <cmath>

int main() {
    int n;
    std::cin >> n;
    std::cout << sqrt(n) << '\n';
    std::cout << sin(n) << '\n';
    std::cout << cos(n) << '\n';
    std::cout << pow(n, 2) << '\n';
    return 0;
}